# 数据集清理

仓库只分发一个运行版本：`expansion/releases/2026-09-10-random-dev-test-v1`。最终版本所有文件清理前后 SHA-256 一致，未修改数据、标签或划分。

旧版本、原始抓取和中间产物已移出开发项目，并保存在开发机器的独立归档中，不随 Git 仓库分发。归档的 `ARCHIVE_MANIFEST.json` 记录原相对路径和最终版本校验和，可供开发机器恢复。

保留三份候选 CSV 和构建代码用于追溯。旧构建脚本依赖其历史输入，重新构建前需自行恢复对应归档。运行当前最终版工作流不依赖这些历史文件。

数据选择器只显示实际存在的数据集版本。历史运行结果仍保留；若要按已归档版本重新评估，需恢复该原始版本，不能替换成另一版数据。
